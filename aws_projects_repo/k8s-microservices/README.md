# Kubernetes Microservices (template)
This folder contains example microservice code (skeletons), Dockerfiles, a Helm chart skeleton, and a Jenkinsfile for CI/CD.

Services included:
- auth-service (Python Flask)
- frontend (static HTML served by nginx)
- order-service (Node.js skeleton)

Helm charts are provided under charts/ for packaging to Kubernetes.
